---
  - name: brew taps for docker
    homebrew:
      name: "{{ item }}"
      state: present
      update_homebrew: yes
      upgrade_all: yes
    with_items:
      - autoconf
      - automake
      - docker
      - docker-compose
      - docker-machine
        # With hyperkit these are probably not needed
        #- docker-machine-driver-xhyve
        #- ocaml
        #- opam
        #- virtualbox
        #
        #

